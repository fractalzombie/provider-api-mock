import { CryptoInterceptor } from 'src/crypto/crypto.interceptor';

describe('CryptoInterceptor', () => {
  it('should be defined', () => {
    // expect(new CryptoInterceptor()).toBeDefined();
  });
});
